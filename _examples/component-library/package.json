{
  "name": "@minipress/example-component-library",
  "version": "0.8.1",
  "private": false,
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev:debug": "node --inspect-brk --no-lazy ./node_modules/.bin/minipress dev",
    "dev": "minipress dev",
    "serve": "minipress serve",
    "gen": "minipress generate",
    "gen:debug": "node --inspect-brk --no-lazy ./node_modules/.bin/minipress generate"
  },
  "files": [
    "src/**/*",
    "!public/*"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@minipress/example-my-component-library": "^0.8.0",
    "@minipress/minipress": "^0.8.1",
    "@zeit-ui/vue": "^2.0.0-alpha.0"
  }
}
