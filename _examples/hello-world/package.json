{
  "name": "hello-world",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev:debug": "node --inspect-brk --no-lazy ./node_modules/.bin/minpress dev --port 8080 --config .minpress/config.js",
    "dev": "minpress dev --port 8080 --config .minpress/config.js",
    "serve": "minpress serve",
    "gen": "minpress generate --config .minpress/config.js",
    "gen:debug": "node --inspect-brk --no-lazy ./node_modules/.bin/minpress generate --config .minpress/config.js"
  },
  "dependencies": {
    "@minpress/minpress2": "^0.1.0"
  }
}
