// @ts-check
const normalizeBuild = require('./build')
const normalizeConfig = require('./config')
const plugin = require('./plugin')

module.exports = {
  normalizeBuild,
  normalizeConfig,
  plugin
}
